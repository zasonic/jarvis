"""Scheduled and background task support.

See ``scheduling.spec.md`` for the contract. The two public pieces are the
pure schedule arithmetic in :mod:`schedule` and the daemon-side
:class:`TaskScheduler` in :mod:`scheduler`.
"""
